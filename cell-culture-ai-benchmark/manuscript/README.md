# Manuscript

This directory contains the preprint PDF, LaTeX source, bibliography, compiled `.bbl`, and submission metadata.

Compile from this directory:

```bash
pdflatex main.tex
pdflatex main.tex
```

The LaTeX source references figures in `../figures/`.
